# Contributing

Keep it calm, accessible, and private:

- No book text, PDFs, or illustrations from any publication.
- All features must work without an account.
- Add keyboard + screen-reader support for every interaction.
- Keep Tamil translations natural; avoid mechanical translation.
- Run `npm run lint`, `npx tsc --noEmit`, `npm run build` before pushing.
